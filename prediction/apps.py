from django.apps import AppConfig


class PredictionConfig(AppConfig):
    """Django app config for the prediction app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "prediction"
